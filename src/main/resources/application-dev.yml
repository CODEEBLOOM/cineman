spring:
  config:
    activate:
      on-profile: dev
  devtools:
    add-properties: true
# kêt nối database
  datasource:
    url: jdbc:sqlserver://localhost:1433;databaseName=cinema_app_db;encrypt=true;trustServerCertificate=true;
    username: sa
    password: 123456
    driver-class-name: com.microsoft.sqlserver.jdbc.SQLServerDriver
  jpa:
    database-platform: org.hibernate.dialect.SQLServer2012Dialect
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        format_sql: false
springdoc:
  api-docs:
    enabled: true
  swagger-ui:
    enabled: true
openapi:
  service:
    api-docs: cineman
    server: http://localhost:${server.port}
    title: Cineman Backend service
    version: 1.0.0

jwt:
  secretKey: son15@2

  spring:
    jpa:
      hibernate:
        ddl-auto=none: